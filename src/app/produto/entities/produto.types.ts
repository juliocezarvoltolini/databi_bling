/**
 * - `S` - Simples 
 * - `V` - Com variações 
 * - `E` - Com composição
 */
export type Formato = 'S' | 'V' | 'E';

/**
 * Define o tipo de rótulo
 * - `C` Rótulos diversos
 * - `V` Rótulos relacionados a variação do produto
 */
export type ProdutoCategoriaTipo = 'C' | 'V'